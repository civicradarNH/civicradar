{
  "name": "civicradar",
  "private": true,
  "description": "CivicRadar PWA — dev-only Playwright + invariant lint (no app build)",
  "scripts": {
    "lint:invariants": "node tools/invariant-lint.js .",
    "test:playwright": "playwright test",
    "test:playwright:ui": "playwright test --ui",
    "test:e2e": "python tests/e2e_comprehensive.py",
    "test:e2e:smoke": "python tests/e2e_comprehensive.py --smoke"
  },
  "devDependencies": {
    "@playwright/test": "1.49.0"
  }
}
